#include "stdafx.h"
#include <iostream>

using namespace std;

int *p;
void mass( int n);
void zap(int n);
void viv(int n);
void del() { delete[] p; }
void change(int n);

int main()
{
	
	int n = 12;
		mass(n);
		zap(n);
		viv(n);
		cout << "_ _ _ _ _ _ _ _ _ _ _ _" << endl;
		change(n);
		viv(n);
		del();
	system("pause");
    return 0;
}

void mass(int n)
{
	p = new int[n];
}

void zap(int n) {
	for (int i = 0; i < n; i++)p[i] = i + 1;
}
void viv(int n) {
	for (int i = 0; i < n; i++) cout << p[i] ;
}
void change(int n) {
	for (int i = 0; i < n; i++)
		if (i % 2 == 0) swap(p[i], p[i + 1]);
		else continue;

}
