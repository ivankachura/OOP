#include "stdafx.h"
#include <iostream>
#include<cmath>
using namespace std;

int main()
{
	double a=-0.6, b=5.3, z=0, t=0;

	
	if (a < b) {
		z = sqrt(abs(a*a - b*b));

	}
	else z=1-2*cos(a)*sin(b);
	if (z < b) {
		t = cbrt(z + a*a*b);
	}
	else if (z == b) {
		t = 1 - log10(z) + cos(a*a*b);
	}
	else t = 1 / cos(z*a);
	cout << a << endl;
	cout << b << endl;
	cout << z << endl;
	cout << t << endl;
	system("pause");
    return 0;
}
