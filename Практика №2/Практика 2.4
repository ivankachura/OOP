#include "stdafx.h"
#include <iostream>
#include<cmath>
using namespace std;

int main()
{
	double a=4.8, b=-7.9, m=0, n=0;

	
	if (a >= b) {
		n = cbrt(a - b);

	}
	else (a*a) + (a - b / sin(a*b));
	if (n < b) {
		m = (n + a / -b) + sqrt(sin(a)*sin(a) - cos(n));
	}
	else if (n == b) {
		m = b*b + tan(n*a);
	}
	else m = pow(b, 3) + n*a*a;
	cout << a << endl;
	cout << b << endl;
	cout << n << endl;
	cout << m << endl;
	system("pause");
    return 0;
}
